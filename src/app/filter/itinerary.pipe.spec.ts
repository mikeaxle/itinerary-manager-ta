import { ItineraryItemFilterPipePipe } from './itinerary.pipe';

describe('ItineraryPipe', () => {
  it('create an instance', () => {
    const pipe = new ItineraryItemFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
