import { InclusionsPipe } from './inclusions.pipe';

describe('InclusionsPipe', () => {
  it('create an instance', () => {
    const pipe = new InclusionsPipe();
    expect(pipe).toBeTruthy();
  });
});
